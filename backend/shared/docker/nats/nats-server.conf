# Local development NATS configuration.
# Host exposure is restricted to 127.0.0.1 by docker-compose.yml.

server_name: ff-local-nats
listen: 0.0.0.0:4222
http: 0.0.0.0:8222

jetstream {
  store_dir: /data/nats/jetstream
}
