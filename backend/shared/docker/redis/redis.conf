# Local development Redis configuration.
# No password is set because docker-compose.yml binds Redis to 127.0.0.1 only.

bind 0.0.0.0
protected-mode no
port 6379
dir /data
appendonly yes
save 60 1000
